{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile && npx prisma generate && npx prisma db push --accept-data-loss && pnpm build && node dist/prisma/seed.js && node dist/prisma/seed-erenkoy.js"
  },
  "deploy": {
    "startCommand": "node dist/src/main",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
