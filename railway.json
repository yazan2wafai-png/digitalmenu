{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && pnpm install --no-frozen-lockfile && npx prisma generate && pnpm build"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma db push --accept-data-loss --skip-generate && node dist/prisma/seed.js && node dist/prisma/seed-erenkoy.js && node dist/src/main",
    "healthcheckPath": "/",
    "healthcheckTimeout": 90,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
